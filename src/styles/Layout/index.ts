import {StyleSheet} from 'react-native';

const LayoutSheet = StyleSheet.create({
  flexContainer: {
    flex: 1,
    alignItems: 'center',
    backgroundColor: '#fff',
  },
  pushToBottom: {
    flex: 1,
  },
});

export default LayoutSheet;

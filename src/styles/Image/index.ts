import {StyleSheet} from 'react-native';

export const ImageSheet = StyleSheet.create({
  generatedImage: {
    resizeMode: 'cover',
    width: 256,
    height: 256,
    marginTop: 24,
    marginBottom: 24,
  },
});

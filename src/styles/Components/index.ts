import {StyleSheet} from 'react-native';

const ComponentSheet = StyleSheet.create({
  textInput: {
    marginTop: 24,
    borderWidth: 1,
    borderColor: 'black',
    width: 300,
    borderRadius: 4,
    paddingHorizontal: 24,
  },
});

export default ComponentSheet;
